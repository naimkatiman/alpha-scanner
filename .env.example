# Database
# SQLite for local dev. For production use a persistent volume or migrate to PostgreSQL.
DATABASE_URL=file:./prisma/dev.db

# NextAuth — required
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# Cron job protection (optional but recommended in production)
CRON_SECRET=your-cron-secret-here

# OpenAI — optional, enables AI signal commentary
OPENAI_API_KEY=

# MetaApi — optional, for broker integration
META_API_TOKEN=
META_API_ACCOUNT_ID=
